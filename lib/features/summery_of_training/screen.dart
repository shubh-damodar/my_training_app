import 'package:flutter/material.dart';

class SummeryOfTrainingScreenScreen extends StatelessWidget {
  final String id;
  const SummeryOfTrainingScreenScreen({
    super.key,
    required this.id,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
